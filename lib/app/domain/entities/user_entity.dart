class UserEntity {
  String? userId, userName, bioInfo, address, email, password, phone, image;
  UserEntity({
    this.userId,
    this.userName,
    this.bioInfo,
    this.address,
    this.email,
    this.password,
    this.phone,
    this.image,
  });
}
